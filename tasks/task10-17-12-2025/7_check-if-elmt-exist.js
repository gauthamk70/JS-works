// 7. Check Element Exists
// Check whether a given number exists in an array or not (without using includes()).

var num = 4
var arr = [1,2,3,4,5,6]
for(let n of arr){
    if(n==num){
        console.log("exist",);
        break
        
    }
}