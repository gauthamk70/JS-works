// Write a program to remove all extra spaces from a string (keep only single spaces between words).

function removeWhiteSpace(str){
    let newStr=str.trim()
    return newStr

}
console.log(removeWhiteSpace("   hello my name is gautham.   "));
