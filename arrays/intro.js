// if you want to store and organize multiple objects with same or different types

//  keeps order
//  duplicates allowed
//  mutable (can update after creation)

var color = ["red","green","blue","yellow"]
console.log(color);
console.log(color[2]);
console.log(color[3]="purple");
console.log(color);


